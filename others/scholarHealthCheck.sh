#!/bin/bash

# check is all containers are runninig from given list.
# if any ofthen is not running  then (do something, but what to do? )
# -- try to find it in [docker ps -a]. if we found it then simple do restart,
# -- check resart status, ? is it success or failed
# -- if failed then we can do remove this container and run respetive docker command. 
